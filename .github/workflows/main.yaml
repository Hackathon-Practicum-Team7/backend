name: App tests workflow

on:
  push:
    branches: [develop]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.9

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8
        pip install -r requirements.txt

    - name: Test with flake8
      run: |
        python -m flake8/
        pytest
  build_and_push_to_docker_hub:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    needs: tests
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to Docker
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Push to Docker Hub
        uses: docker/build-push-action@v4
        with:
          push: true
          context: ./
          tags: ${{ secrets.DOCKER_USERNAME }}/hakaton:latest
      
  deploy:
    runs-on: ubuntu-latest
    needs: build_and_push_to_docker_hub
    if: github.ref == 'refs/heads/develop'
    steps:
      - name: executing remote ssh commands to deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd hakaton/
            sudo docker compose stop
            sudo docker rm backend         
            sudo docker rm nginx
            sudo docker rmi ${{ secrets.DOCKER_USERNAME }}/hakaton
            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/hakaton:latest
            sudo docker compose -f docker-compose.yml up -d
            sudo docker exec -i backend python manage.py migrate
            sudo docker exec -i backend python manage.py collectstatic --noinput
